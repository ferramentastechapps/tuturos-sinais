., kk\// AuthDialog is no longer used — login is handled by /login page.
// Kept as empty export to avoid breaking any lingering imports.

export function AuthDialog(_props: { open?: boolean; onOpenChange?: (open: boolean) => void }) {
  return null;
}
